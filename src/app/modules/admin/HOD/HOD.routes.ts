import { Routes } from '@angular/router';
import { ListHODComponent } from './list-hod/list-hod.component';

export default [
    {
        path: 'list',
        component: ListHODComponent,
    }
] as Routes;